# ios/ — declarative Xcode inputs (no project yet)

Entitlements, the extension Info.plist and the engine-bootstrap note for the packet tunnel target
(principal class = Kotlin `UccPacketTunnelProvider`). The Xcode project itself, the K/N framework
export of `:core:ios-vpn`, and Libbox linkage are Phase 7 (see docs/IOS_NETWORK_EXTENSION.md).
Nothing here is signed or built in CI.
