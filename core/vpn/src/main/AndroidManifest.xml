<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Per-app routing (Settings → Routing → include/exclude apps) lists every
      installed app that holds INTERNET via PackageManager.getInstalledApplications
      (app/ui/settings/InstalledApps.kt) and passes the chosen packages to
      VpnService.Builder.addAllowed/DisallowedApplication. On API 30+ that
      enumeration needs broad package visibility; a <queries> filter cannot
      express "every app that can use the network". Play-policy implications:
      docs/PLAY_STORE_CHECKLIST.md §QUERY_ALL_PACKAGES.
      The app declares no RECEIVE_BOOT_COMPLETED and has no boot receiver (always-on
      VPN is handled by the system via SUPPORTS_ALWAYS_ON below). The merged manifest
      still contains it: androidx.work contributes it for its RescheduleReceiver, which
      re-arms the periodic subscription refresh after a reboot (WorkManager jobs are
      scheduled with setPersisted(false)). Retained deliberately; see docs/RELEASE.md
      §Permissions for the approval record and CI enforcement.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application>
        <!--
          exported=true is REQUIRED by the platform: the system binds with BIND_VPN_SERVICE,
          and the permission attribute ensures nothing else can bind or start it.
        -->
        <service
            android:name=".UcVpnService"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>
    </application>
</manifest>
